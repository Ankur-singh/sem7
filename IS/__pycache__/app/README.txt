The Client-Server program is made using FLASK (a python framework for creating websites).
MD5 is used for the server side encryption technique. 
The front end is made using Jinja (HTML rendering Python framework).

Execution :-
-----------------------------------

1. python app.py - (python program acts as a server).

then open browser and goto "localhost" - (browser acts as a client).
 
